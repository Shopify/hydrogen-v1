---
'@shopify/hydrogen': patch
---

`<Image/>` component has improved handling for `width` as a string. It also corrects an issue with the `scale` prop and its usage with the Shopify CDN. The generated `srcset` is also updated and improved.
